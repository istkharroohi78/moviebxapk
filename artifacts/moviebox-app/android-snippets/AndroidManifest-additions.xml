<!--
  MOVIE BOX — add these to android/app/src/main/AndroidManifest.xml
  after running `npx cap add android`.

  1) Inside <manifest> (above <application>):
-->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="28" />
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

<!--
  2) On the <activity android:name=".MainActivity" ...> tag add these
     attributes so fullscreen / rotation does not restart the player:

     android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|locale|layoutDirection|fontScale|uiMode"
     android:hardwareAccelerated="true"
     android:screenOrientation="fullUser"

  3) On the <application> tag:
     android:usesCleartextTraffic="true"
     android:hardwareAccelerated="true"
-->
